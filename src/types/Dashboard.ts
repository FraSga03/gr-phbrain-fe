export type Contribution = {
    contribution: string;
    total: number;
    active: number;
}

export type UserRank = {
    username: string;
    usage: number;
    trust: number;
};

export type Credit = {
    bonus: number;
    credits: number;
}